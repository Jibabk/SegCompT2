# Variáveis de configuração
var HOME_NET any
var EXTERNAL_NET any

# Configuração dos decodificadores e pré-processadores
# (Mantendo o padrão para simplicidade)

# Caminho para as regras
var RULE_PATH ./

# Incluir nossas regras customizadas
include $RULE_PATH/local.rules
